{
  "name": "attendance-app",
  "version": "1.0.0",
  "description": "نظام الحضور والانصراف — مجموعة الفتح",
  "private": true,
  "scripts": {
    "build": "echo \"No build step — static HTML in www/\"",
    "cap:sync": "npx cap sync android && powershell -ExecutionPolicy Bypass -File scripts/patch-android-manifest.ps1",
    "cap:open": "npx cap open android",
    "patch:android": "powershell -ExecutionPolicy Bypass -File scripts/patch-android-manifest.ps1"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/geolocation": "^7.0.0",
    "@capacitor/local-notifications": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "^5.8.3"
  }
}
