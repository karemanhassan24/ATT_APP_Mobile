{
  "name": "attendance-app",
  "version": "1.0.0",
  "description": "نظام الحضور والانصراف — مجموعة الفتح",
  "private": true,
  "scripts": {
    "build": "echo \"No build step — static HTML in www/\"",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/core": "^7.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0",
    "typescript": "^5.8.3"
  }
}
